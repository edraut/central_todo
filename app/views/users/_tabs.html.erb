<div class="span-16 last">
  <div class="list_action attach_above last<% if action_name == 'show' %> selected<% end %>"><%= link_to 'account info', user_url(@user) %></div>
  <div class="list_action attach_above<% if action_name == 'settings' %> selected<% end %>"><%= link_to 'settings', settings_user_url(@user) %></div>
</div>
