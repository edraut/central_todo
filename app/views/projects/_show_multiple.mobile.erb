<div id="multiple_projects_page_<%= @projects.sort{|a,b| a.id <=> b.id}.map{|t| t.id}.join('_')%>" data-children_pages="({})">
<% for @project in @projects %>
<%= render :partial => 'show_full' %>
<% end %>
</div>