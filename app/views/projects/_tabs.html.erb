<div class="span-16 last">
  <div class="list_action attach_above last<% if action_name == 'edit' %> selected<% end %>"><%= link_to 'plan info', edit_plan_url(@project) %></div>
  <div class="list_action attach_above<% if action_name == 'comments' %> selected<% end %>"><%= link_to "comments#{@task.comments.count}", comments_plan_url(@project) %></div>
  <div class="list_action attach_above<% if ['show','sort_tasks'].include? action_name %> selected<% end %>"><%= link_to 'task list', plan_url(@project) %></div>
</div>
