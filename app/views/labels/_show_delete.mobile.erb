<div class="row_item delete list_action" >
<%= form_for @label, :url => label_url(@label) do |f| %>
<div onclick="void(0)" data-role="delete" data-confirm="Are you sure you want to delete this label?">&otimes;</div>
<% end %>
</div>
<div class="row_item last list_action line_item_height">
delete label
</div>