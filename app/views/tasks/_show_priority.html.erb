<%= form_for @task, :html => {
	:'data-ajax_behavior' => 'ajax_form',
	:'data-ajax_method' => 'PUT',
	:'data-ajax_success_element' => "task_priority",
	:id => 'edit_task_priority'} do |f| %><%= f.hidden_field :priority, :id => "priority_#{@task.id}", :'data-behavior' => 'auto_submit' %><div data-behavior="check_box_proxy" data-check_box="priority_<%= @task.id %>" class="check_box_proxy in_header <% if @task.priority? %>checked<% end %>">&lowast;</div><%= hidden_field_tag 'attribute', 'priority' %>
	<% end %>